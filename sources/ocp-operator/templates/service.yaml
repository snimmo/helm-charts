apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocp-operator.fullname" . }}
  labels:
    {{- include "ocp-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocp-operator.selectorLabels" . | nindent 4 }}
